<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:background = "#2C0404"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StartScreen">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Basement"
        android:textColor="#DBB376"
        android:textSize="80dp"
        android:paddingTop = "100dp"
        app:layout_constraintTop_toTopOf = "parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        android:id = "@+id/basement"/>

    <TextView
        android:id = "@+id/crawler"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Crawler"
        android:textColor="#DBB376"
        android:textSize="80dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/basement"
        android:paddingBottom = "100dp"
        />
    <!--this button will take to MainActivity screen-->

    <Button
        android:id="@+id/StartButton"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:text = "Let's Escape!"
        android:textColor = "#2C0404"
        android:textSize = "20dp"
        android:backgroundTint="#DBB376"
        android:onClick="mainActivity"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/crawler" />

    <Button
        android:id="@+id/QuitButton"
        android:layout_width="200dp"
        android:layout_height="100dp"
        android:text = "I'll stay trapped..."
        android:textColor = "#2C0404"
        android:textSize = "20dp"
        android:backgroundTint="#DBB376"
        android:onClick="MainActivity"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@id/StartButton" />

</androidx.constraintlayout.widget.ConstraintLayout>